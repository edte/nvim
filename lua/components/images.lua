-- try_require('numb').setup()

-- -- try_require('hologram').setup {
-- --     auto_display = true -- WIP automatic markdown image display, may be prone to breaking
-- -- }

-- try_require('telescope').load_extension('media_files')

-- try_require 'telescope'.setup {
--     extensions = {
--         media_files = {
--             -- filetypes whitelist
--             -- defaults to {"png", "jpg", "mp4", "webm", "pdf"}
--             filetypes = { "png", "webp", "jpg", "jpeg" },
--             -- find command (defaults to `fd`)
--             find_cmd = "rg"
--         }
--     },
-- }
